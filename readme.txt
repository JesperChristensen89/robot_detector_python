Python script developed to handle the image processing.
OpenCV needs to be installed in order to run this script.

The script is evaluating an image, which path needs to be set from within the script.